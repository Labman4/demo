name: Prepare build (storage)
on:
  workflow_dispatch:
  pull_request:
    branches:
    - main 
  push:
    branches:
    - native-image
    paths:
      -  'storage/storage-server/**'
jobs:
  native:
    uses: Labman4/workflow/.github/workflows/native-image-base.yml@native-image
    secrets: inherit
    with:
      module: storage
      modulePath: storage/storage-server
      
 

    
          
    
          

    
  
